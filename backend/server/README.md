# server/

Intentionally empty. `langgraph dev` (see `../langgraph.json`) *is* the server for this project —
there's no custom FastAPI/Flask wrapper here. The graph definition lives in `../graph/graph.py`.
